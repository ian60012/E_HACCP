# 肉品加工：升級與驗證

## 功能

正式生產新增 `meat_processing`。使用既有產品、批次、角色、庫存與簽名元件，新增逐版本保存的肉品加工明細。

1. 在產品管理建立「肉品加工」產品，例如豬肉分切。
2. 為各產出建立以 `kg`／`公斤` 管理的半成品或成品，設定允許庫位。也可在原料管理由 Admin/Captain 將既有 KG 原料一鍵啟用為肉品半成品或成品；SKU 與 `raw` 主分類保持不變。
3. 如有需要，建立明確適用「肉品加工」的包裝類型；`both` 仍只適用成型與熱加工。
4. 在肉品加工入口建立批次、填寫原料與工序、記錄各項產出及損耗。投入包含醃料及水；溫度欄位只保存實測值。
5. 投入選擇來源庫位；啟用批號管理的品項還須選來源 lot。儲存明細版本，完成簽名送審；QA 簽名後鎖定，再辦理扣料與入庫。已覆核資料更正須作廢重建。

肉品入庫會在單一交易建立投入 OUT 與產出 IN 兩張過帳單。啟用批號管理的庫存按 SKU、庫位與 lot 分開，頁面仍提供 SKU/庫位彙總。不提供效期／FEFO、成本分攤、自動標籤或自動排程轉單。

## 升級步驟

部署時應使用既有正式環境流程；本次實作驗證未修改正式環境。

1. 暫停生產與庫存寫入，備份 PostgreSQL、Production Helper JSON volume、環境設定及目前 Git 版本。檢查備份可讀後才升級。
2. 先在資料庫副本依序套用 `database/migrations/20260915_meat_processing.sql` 與 `database/migrations/20260915_inventory_lots.sql`，確認既有批次數量與 SKU/庫位彙總餘額一致。升級腳本可重複執行，不刪除舊資料。
3. 查詢 `SELECT * FROM prod_batch_type_migration_review;`，記錄無對應產品的歷史批次。這些批次保留 NULL 類型與原本的產品查詢行為，不自動猜測分類。
4. 建置並更新後端與前端。後端啟動會先執行原有 migrations，再依序執行肉品與 lot 升級 SQL；新 enum 值會先單獨提交。
5. 核對三個產品類型及 `open / packed / closed` 批次 enum；肉品四個操作狀態保存在肉品主記錄，不另擴充批次狀態。
6. 升級不會自行判斷哪些原料應轉換。由 Admin/Captain 在原料管理逐項選擇肉品半成品或成品並啟用，核對系統產生的 `LEGACY-{SKU}-{日期}` lot 與啟用前各庫位數量完全相同。
7. 檢查健康端點、肉品入口、lot 展開庫存、既有成型／熱加工頁面，以及各角色權限，再恢復寫入。

不使用 `docker compose down -v`。升級為增量新增結構；需要回退應停寫並依備份恢復相容的程式及資料庫，不移除已產生的肉品記錄或 enum 值。

## 測試

後端使用 `backend/requirements.txt` 的版本。提供隔離 PostgreSQL 管理連線，測試會建立隨機名稱的資料庫副本，並在結束時只刪除自身建立的資料庫。

```powershell
$env:PYTHONPATH = 'backend'
$env:TEST_MEAT_ADMIN_URL = 'postgresql://postgres@127.0.0.1:55439/postgres'
python -m pytest backend/tests/test_meat_processing.py -q
npm run build --prefix frontend
```

測試連線須指向測試 cluster，使用可建立資料庫的帳號。未設定 `TEST_MEAT_ADMIN_URL` 時只執行純 schema／SQL 同步檢查，整合測試會明確標示 skipped。

涵蓋：

- 舊 schema 含成型、熱加工與無法對應產品的批次，啟動升級兩次後再複製為每項測試的獨立資料庫。
- 多原料、醃料增重、多產出、多庫位、差額說明、來源 lot、公斤單位及時間驗證。
- 明細歷史、過期版本、缺少簽名、角色限制、覆核鎖定與舊 API 繞過防護。
- 一鍵啟用、期初 lot 數量守恆、收貨供應商／自動 lot、人工 lot 出庫、逐 lot 盤點與現場發現 lot。
- 同 SKU 投入產出、重複入庫、兩個批次同時入庫、實際過帳後發生錯誤的完整交易回滾，以及已使用產出 lot 時拒絕作廢。
- 產品匯入與原有成型／熱加工包數入庫回歸。

本機另以 PostgreSQL 16.13 驗證完整 `database/init.sql` 與後端啟動。瀏覽器驗證涵蓋 390px 手機與桌面寬度、儲存新版本、實際繪製簽名、QA 鎖定、入庫，以及舊批次 URL 導向肉品頁面。

庫存寫入使用資料表交易鎖序列化過帳、作廢及盤點確認；在大量並行庫存寫入環境部署前，應依實際負載觀察等待時間。讀取庫存餘額不會被此鎖阻擋。

## 本次驗證結果（2026-09-15）

- 25 項後端測試通過；包含隔離 PostgreSQL 資料庫複製、重複升級、lot 庫存、權限、交易與原有生產入庫回歸。
- TypeScript 與 Vite 正式建置通過。
- 桌面及 390px 手機流程通過，手機無橫向溢出，肉品新批次不顯示單顆重量。
- 新批次日期改用既有墨爾本時區，瀏覽器確認澳洲上午顯示當地當日，而非 UTC 前一天。
- 已於 2026-09-15 部署至 NAS 正式環境；部署前備份位於 `/volume1/docker/E_HACCP/backups/20260915_094656`。
- 正式環境資料升級完成，無法對應的舊批次為 0；後端健康檢查與前端首頁均回傳 HTTP 200。
