# E_HACCP Codex Entry

Before developing this project, read `docs/codex_project_context.md`.
If present, also read local-only `docs/codex_local_runtime_memory.md`; it is for this machine only and must not be committed or uploaded.

Use it as the project-local onboarding skill:

1. Start with the "Quick Load" section.
2. Open the module map that matches the requested change.
3. Prefer existing backend router/schema/model/service and frontend api/type/page patterns.
4. Check the "Known Drift" section before trusting older README/docs statements.
5. Update `docs/codex_project_context.md` whenever architecture, routes, persistence, roles, or validation rules change.
